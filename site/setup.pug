#setup.main-section(hidden="")
  h2 Setup
  h4 Installation Options:
  button.accordion(onclick="letsGo.toggle('#npm')")
    h5 npm
  #npm.panel(hidden="")
    .fake-code npm install letsgo
  button.accordion(onclick="letsGo.toggle('#bower')")
    h5 Bower
  #bower.panel(hidden="")
    .fake-code bower install letsgo
  button.accordion(onclick="letsGo.toggle('#manual')")
    h5 Manual Download
  #manual.panel(hidden="")
    p.bottom-margin Download a letsGo .zip via #[+external(href="https://github.com/Beg-in/letsGo/releases/latest", title="letsGo Latest Release") GitHub], then place the unzipped folder inside of your project's folder.
  button.accordion(onclick="letsGo.toggle('#cdn')")
    h5 CDN
  #cdn.panel(hidden="")
    p Get the latest version of letsGo from #[+external(href="https://www.jsdelivr.com", title="jsDelivr cdn") jsDelivr]:
    pre.fake-code.mb-0!= hl(`<script src="https://cdn.jsdelivr.net/npm/letsgo@${props.version}" async></script>`)
    p Place that script code in the head of your document.
    p A list of all releases is available on #[+external(href="https://github.com/Beg-in/letsGo/releases", title="letsGo Releases") GitHub].
  h5 One last thing...
  p Paste this class into your css. If you're already using #[+external(href="https://frowcss.com", title="Frow CSS") Frow CSS] skip this step.
  pre.fake-code.mb-0
    include:hl:pug(pretty=true) ../examples/style
